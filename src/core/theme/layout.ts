import { spacing } from '@/core/theme/tokens';

/** Horizontal inset for all screen content. */
export const SCREEN_HORIZONTAL_PADDING = spacing.lg;

/** Extra space above the bottom tab bar when scrolling lists. */
export const LIST_BOTTOM_PADDING = spacing.xl;
